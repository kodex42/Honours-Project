extends DirectionalLight

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	# 10 seconds is 1 degree of rotation
#	var rot = deg2rad(delta / 10)
#	rotate_z(rot)
	pass
